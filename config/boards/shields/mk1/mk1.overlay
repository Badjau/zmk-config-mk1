/ {
    zephyr,console = &uart0;

    &mk1 {
        compatible = "zmk,keyboard";
        matrix {
            rows = <1>;
            cols = <1>;
            row-gpios = <&pro_micro 0 GPIO_ACTIVE_LOW>;
            col-gpios = <&pro_micro 1 GPIO_ACTIVE_LOW>;
        };
    };
};